/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { TeeService } from "../tee.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { TeeCreateInput } from "./TeeCreateInput";
import { TeeWhereInput } from "./TeeWhereInput";
import { TeeWhereUniqueInput } from "./TeeWhereUniqueInput";
import { TeeFindManyArgs } from "./TeeFindManyArgs";
import { TeeUpdateInput } from "./TeeUpdateInput";
import { Tee } from "./Tee";
import { HoleFindManyArgs } from "../../hole/base/HoleFindManyArgs";
import { Hole } from "../../hole/base/Hole";
import { HoleWhereUniqueInput } from "../../hole/base/HoleWhereUniqueInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class TeeControllerBase {
  constructor(
    protected readonly service: TeeService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Tee })
  @nestAccessControl.UseRoles({
    resource: "Tee",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async create(@common.Body() data: TeeCreateInput): Promise<Tee> {
    return await this.service.create({
      data: {
        ...data,

        course: data.course
          ? {
              connect: data.course,
            }
          : undefined,
      },
      select: {
        id: true,
        name: true,
        gender: true,
        par: true,
        rating: true,
        slope: true,
        frating: true,
        fslope: true,
        brating: true,
        bslope: true,
        createdAt: true,
        updatedAt: true,

        course: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [Tee] })
  @ApiNestedQuery(TeeFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "Tee",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findMany(@common.Req() request: Request): Promise<Tee[]> {
    const args = plainToClass(TeeFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        id: true,
        name: true,
        gender: true,
        par: true,
        rating: true,
        slope: true,
        frating: true,
        fslope: true,
        brating: true,
        bslope: true,
        createdAt: true,
        updatedAt: true,

        course: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Tee })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Tee",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findOne(
    @common.Param() params: TeeWhereUniqueInput
  ): Promise<Tee | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        id: true,
        name: true,
        gender: true,
        par: true,
        rating: true,
        slope: true,
        frating: true,
        fslope: true,
        brating: true,
        bslope: true,
        createdAt: true,
        updatedAt: true,

        course: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Tee })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Tee",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async update(
    @common.Param() params: TeeWhereUniqueInput,
    @common.Body() data: TeeUpdateInput
  ): Promise<Tee | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          course: data.course
            ? {
                connect: data.course,
              }
            : undefined,
        },
        select: {
          id: true,
          name: true,
          gender: true,
          par: true,
          rating: true,
          slope: true,
          frating: true,
          fslope: true,
          brating: true,
          bslope: true,
          createdAt: true,
          updatedAt: true,

          course: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Tee })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Tee",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async delete(
    @common.Param() params: TeeWhereUniqueInput
  ): Promise<Tee | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          id: true,
          name: true,
          gender: true,
          par: true,
          rating: true,
          slope: true,
          frating: true,
          fslope: true,
          brating: true,
          bslope: true,
          createdAt: true,
          updatedAt: true,

          course: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/hole")
  @ApiNestedQuery(HoleFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "Hole",
    action: "read",
    possession: "any",
  })
  async findManyHole(
    @common.Req() request: Request,
    @common.Param() params: TeeWhereUniqueInput
  ): Promise<Hole[]> {
    const query = plainToClass(HoleFindManyArgs, request.query);
    const results = await this.service.findHole(params.id, {
      ...query,
      select: {
        id: true,
        holeNumber: true,
        par: true,
        handicap: true,
        length: true,
        createdAt: true,
        updatedAt: true,

        tee: {
          select: {
            id: true,
          },
        },
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/hole")
  @nestAccessControl.UseRoles({
    resource: "Tee",
    action: "update",
    possession: "any",
  })
  async connectHole(
    @common.Param() params: TeeWhereUniqueInput,
    @common.Body() body: HoleWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      hole: {
        connect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/hole")
  @nestAccessControl.UseRoles({
    resource: "Tee",
    action: "update",
    possession: "any",
  })
  async updateHole(
    @common.Param() params: TeeWhereUniqueInput,
    @common.Body() body: HoleWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      hole: {
        set: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/hole")
  @nestAccessControl.UseRoles({
    resource: "Tee",
    action: "update",
    possession: "any",
  })
  async disconnectHole(
    @common.Param() params: TeeWhereUniqueInput,
    @common.Body() body: HoleWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      hole: {
        disconnect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }
}
