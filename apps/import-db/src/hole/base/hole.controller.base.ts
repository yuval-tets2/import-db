/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { HoleService } from "../hole.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { HoleCreateInput } from "./HoleCreateInput";
import { HoleWhereInput } from "./HoleWhereInput";
import { HoleWhereUniqueInput } from "./HoleWhereUniqueInput";
import { HoleFindManyArgs } from "./HoleFindManyArgs";
import { HoleUpdateInput } from "./HoleUpdateInput";
import { Hole } from "./Hole";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class HoleControllerBase {
  constructor(
    protected readonly service: HoleService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Hole })
  @nestAccessControl.UseRoles({
    resource: "Hole",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async create(@common.Body() data: HoleCreateInput): Promise<Hole> {
    return await this.service.create({
      data: {
        ...data,

        tee: {
          connect: data.tee,
        },
      },
      select: {
        id: true,
        holeNumber: true,
        par: true,
        handicap: true,
        length: true,
        createdAt: true,
        updatedAt: true,

        tee: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [Hole] })
  @ApiNestedQuery(HoleFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "Hole",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findMany(@common.Req() request: Request): Promise<Hole[]> {
    const args = plainToClass(HoleFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        id: true,
        holeNumber: true,
        par: true,
        handicap: true,
        length: true,
        createdAt: true,
        updatedAt: true,

        tee: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Hole })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Hole",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findOne(
    @common.Param() params: HoleWhereUniqueInput
  ): Promise<Hole | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        id: true,
        holeNumber: true,
        par: true,
        handicap: true,
        length: true,
        createdAt: true,
        updatedAt: true,

        tee: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Hole })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Hole",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async update(
    @common.Param() params: HoleWhereUniqueInput,
    @common.Body() data: HoleUpdateInput
  ): Promise<Hole | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          tee: {
            connect: data.tee,
          },
        },
        select: {
          id: true,
          holeNumber: true,
          par: true,
          handicap: true,
          length: true,
          createdAt: true,
          updatedAt: true,

          tee: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Hole })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Hole",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async delete(
    @common.Param() params: HoleWhereUniqueInput
  ): Promise<Hole | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          id: true,
          holeNumber: true,
          par: true,
          handicap: true,
          length: true,
          createdAt: true,
          updatedAt: true,

          tee: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
