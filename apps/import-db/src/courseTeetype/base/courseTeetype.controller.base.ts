/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { CourseTeetypeService } from "../courseTeetype.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { CourseTeetypeCreateInput } from "./CourseTeetypeCreateInput";
import { CourseTeetypeWhereInput } from "./CourseTeetypeWhereInput";
import { CourseTeetypeWhereUniqueInput } from "./CourseTeetypeWhereUniqueInput";
import { CourseTeetypeFindManyArgs } from "./CourseTeetypeFindManyArgs";
import { CourseTeetypeUpdateInput } from "./CourseTeetypeUpdateInput";
import { CourseTeetype } from "./CourseTeetype";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class CourseTeetypeControllerBase {
  constructor(
    protected readonly service: CourseTeetypeService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: CourseTeetype })
  @nestAccessControl.UseRoles({
    resource: "CourseTeetype",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async create(
    @common.Body() data: CourseTeetypeCreateInput
  ): Promise<CourseTeetype> {
    return await this.service.create({
      data: {
        ...data,

        course: {
          connect: data.course,
        },
      },
      select: {
        id: true,
        teeId: true,
        teeType: true,
        createdAt: true,
        updatedAt: true,

        course: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [CourseTeetype] })
  @ApiNestedQuery(CourseTeetypeFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "CourseTeetype",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findMany(@common.Req() request: Request): Promise<CourseTeetype[]> {
    const args = plainToClass(CourseTeetypeFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        id: true,
        teeId: true,
        teeType: true,
        createdAt: true,
        updatedAt: true,

        course: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: CourseTeetype })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CourseTeetype",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findOne(
    @common.Param() params: CourseTeetypeWhereUniqueInput
  ): Promise<CourseTeetype | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        id: true,
        teeId: true,
        teeType: true,
        createdAt: true,
        updatedAt: true,

        course: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: CourseTeetype })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CourseTeetype",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async update(
    @common.Param() params: CourseTeetypeWhereUniqueInput,
    @common.Body() data: CourseTeetypeUpdateInput
  ): Promise<CourseTeetype | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          course: {
            connect: data.course,
          },
        },
        select: {
          id: true,
          teeId: true,
          teeType: true,
          createdAt: true,
          updatedAt: true,

          course: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: CourseTeetype })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CourseTeetype",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async delete(
    @common.Param() params: CourseTeetypeWhereUniqueInput
  ): Promise<CourseTeetype | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          id: true,
          teeId: true,
          teeType: true,
          createdAt: true,
          updatedAt: true,

          course: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
