/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { RandomplayerService } from "../randomplayer.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { RandomplayerCreateInput } from "./RandomplayerCreateInput";
import { RandomplayerWhereInput } from "./RandomplayerWhereInput";
import { RandomplayerWhereUniqueInput } from "./RandomplayerWhereUniqueInput";
import { RandomplayerFindManyArgs } from "./RandomplayerFindManyArgs";
import { RandomplayerUpdateInput } from "./RandomplayerUpdateInput";
import { Randomplayer } from "./Randomplayer";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class RandomplayerControllerBase {
  constructor(
    protected readonly service: RandomplayerService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Randomplayer })
  @nestAccessControl.UseRoles({
    resource: "Randomplayer",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async create(
    @common.Body() data: RandomplayerCreateInput
  ): Promise<Randomplayer> {
    return await this.service.create({
      data: data,
      select: {
        RandomplayerId: true,
        lname: true,
        fname: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [Randomplayer] })
  @ApiNestedQuery(RandomplayerFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "Randomplayer",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findMany(@common.Req() request: Request): Promise<Randomplayer[]> {
    const args = plainToClass(RandomplayerFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        RandomplayerId: true,
        lname: true,
        fname: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Randomplayer })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Randomplayer",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findOne(
    @common.Param() params: RandomplayerWhereUniqueInput
  ): Promise<Randomplayer | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        RandomplayerId: true,
        lname: true,
        fname: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Randomplayer })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Randomplayer",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async update(
    @common.Param() params: RandomplayerWhereUniqueInput,
    @common.Body() data: RandomplayerUpdateInput
  ): Promise<Randomplayer | null> {
    try {
      return await this.service.update({
        where: params,
        data: data,
        select: {
          RandomplayerId: true,
          lname: true,
          fname: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Randomplayer })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Randomplayer",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async delete(
    @common.Param() params: RandomplayerWhereUniqueInput
  ): Promise<Randomplayer | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          RandomplayerId: true,
          lname: true,
          fname: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
