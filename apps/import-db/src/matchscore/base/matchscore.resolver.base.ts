/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import * as apollo from "apollo-server-express";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { CreateMatchscoreArgs } from "./CreateMatchscoreArgs";
import { UpdateMatchscoreArgs } from "./UpdateMatchscoreArgs";
import { DeleteMatchscoreArgs } from "./DeleteMatchscoreArgs";
import { MatchscoreCountArgs } from "./MatchscoreCountArgs";
import { MatchscoreFindManyArgs } from "./MatchscoreFindManyArgs";
import { MatchscoreFindUniqueArgs } from "./MatchscoreFindUniqueArgs";
import { Matchscore } from "./Matchscore";
import { Member } from "../../member/base/Member";
import { Schedule } from "../../schedule/base/Schedule";
import { MatchscoreService } from "../matchscore.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => Matchscore)
export class MatchscoreResolverBase {
  constructor(
    protected readonly service: MatchscoreService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "Matchscore",
    action: "read",
    possession: "any",
  })
  async _matchscoresMeta(
    @graphql.Args() args: MatchscoreCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [Matchscore])
  @nestAccessControl.UseRoles({
    resource: "Matchscore",
    action: "read",
    possession: "any",
  })
  async matchscores(
    @graphql.Args() args: MatchscoreFindManyArgs
  ): Promise<Matchscore[]> {
    return this.service.findMany(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => Matchscore, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "Matchscore",
    action: "read",
    possession: "own",
  })
  async matchscore(
    @graphql.Args() args: MatchscoreFindUniqueArgs
  ): Promise<Matchscore | null> {
    const result = await this.service.findOne(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => Matchscore)
  @nestAccessControl.UseRoles({
    resource: "Matchscore",
    action: "create",
    possession: "any",
  })
  async createMatchscore(
    @graphql.Args() args: CreateMatchscoreArgs
  ): Promise<Matchscore> {
    return await this.service.create({
      ...args,
      data: {
        ...args.data,

        members: args.data.members
          ? {
              connect: args.data.members,
            }
          : undefined,

        schedule: args.data.schedule
          ? {
              connect: args.data.schedule,
            }
          : undefined,
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => Matchscore)
  @nestAccessControl.UseRoles({
    resource: "Matchscore",
    action: "update",
    possession: "any",
  })
  async updateMatchscore(
    @graphql.Args() args: UpdateMatchscoreArgs
  ): Promise<Matchscore | null> {
    try {
      return await this.service.update({
        ...args,
        data: {
          ...args.data,

          members: args.data.members
            ? {
                connect: args.data.members,
              }
            : undefined,

          schedule: args.data.schedule
            ? {
                connect: args.data.schedule,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => Matchscore)
  @nestAccessControl.UseRoles({
    resource: "Matchscore",
    action: "delete",
    possession: "any",
  })
  async deleteMatchscore(
    @graphql.Args() args: DeleteMatchscoreArgs
  ): Promise<Matchscore | null> {
    try {
      return await this.service.delete(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => Member, {
    nullable: true,
    name: "members",
  })
  @nestAccessControl.UseRoles({
    resource: "Member",
    action: "read",
    possession: "any",
  })
  async resolveFieldMembers(
    @graphql.Parent() parent: Matchscore
  ): Promise<Member | null> {
    const result = await this.service.getMembers(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => Schedule, {
    nullable: true,
    name: "schedule",
  })
  @nestAccessControl.UseRoles({
    resource: "Schedule",
    action: "read",
    possession: "any",
  })
  async resolveFieldSchedule(
    @graphql.Parent() parent: Matchscore
  ): Promise<Schedule | null> {
    const result = await this.service.getSchedule(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
